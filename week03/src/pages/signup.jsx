import styled from "styled-components";

const SignUp = () => {
    return(
        <SignUpPage>
            <h1>회원가입 페이지</h1>
        </SignUpPage>

    );
};

const SignUpPage = styled.div`
    color: #f0f0f0;
`

export default SignUp;