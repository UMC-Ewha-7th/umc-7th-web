import styled from "styled-components";

const Login = () => {
    return(
        <LoginP>
            <h1>로그인 페이지</h1>
        </LoginP>

    );
};

const LoginP = styled.div`
    color:#f0f0f0;
`

export default Login;