import styled from "styled-components";

const SearchPage = () => {
    return(
        <SearchP>
            <h1>검색페이지</h1>
        </SearchP>
    );
};

const SearchP = styled.div`
    color: #f0f0f0;
`

export default SearchPage;