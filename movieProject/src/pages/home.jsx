import styled from "styled-components";

const HomePage = () => {
    return(
        <HomeP>
            <h1>Home Page</h1>
        </HomeP>

    );
};

const HomeP = styled.div`
    color: white;
`

export default HomePage;